import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cs.salesforce.chat',
  appName: 'cs-salesforce-chat',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
